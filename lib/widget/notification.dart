import 'package:flutter/material.dart';

class notification_none extends StatelessWidget {
  const notification_none({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("h"),);
  }
}
